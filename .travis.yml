language: minimal

dist: bionic

addons:
   apt:
      packages:
         - debhelper
         - devscripts
         - fakeroot

script: debuild -us -uc

deploy:
   provider: releases
   api_key: "$GITHUB_TOKEN"
   file:
      - "../sharp-configs_0.5.4.dsc"
      - "../sharp-configs_0.5.4.tar.xz"
      - "../sharp-configs_0.5.4_all.deb"
   skip_cleanup: true
   on:
      tags: true

