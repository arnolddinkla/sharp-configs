language: minimal

dist: bionic

addons:
   apt:
      packages:
         - debhelper
         - devscripts
         - fakeroot

script: debuild -us -uc

deploy:
   provider: releases
   api_key: "$GITHUB_TOKEN"
   file:
      - "../sharp-configs_0.4.1.dsc"
      - "../sharp-configs_0.4.1.tar.xz"
      - "../sharp-configs_0.4.1_all.deb"
   skip_cleanup: true
   on:
      tags: true

