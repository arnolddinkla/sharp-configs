language: minimal

dist: bionic

addons:
   apt:
      packages:
         - debhelper
         - devscripts
         - fakeroot

script: debuild -us -uc

deploy:
   provider: releases
   api_key: "$GITHUB_TOKEN"
   file:
      - "../sharp-configs_0.5.2.dsc"
      - "../sharp-configs_0.5.2.tar.xz"
      - "../sharp-configs_0.5.2_all.deb"
   skip_cleanup: true
   on:
      tags: true

