language: minimal

dist: bionic

addons:
   apt:
      packages:
         - debhelper
         - devscripts
         - fakeroot

script: debuild -us -uc

deploy:
   provider: releases
   api_key: "$GITHUB_TOKEN"
   file:
      - "../sharp-configs_*.dsc"
      - "../sharp-configs_*.tar.xz"
      - "../sharp-configs_*_all.deb"
   skip_cleanup: true
   on:
      tags: true

